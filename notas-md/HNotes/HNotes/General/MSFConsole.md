# MSFConsole

- To add pivot subnets:

```
run autoroute -s 10.10.10.1/24
```

- Search if db_autopwn is still usable

<!-- -->

- `analyze` to search exploits

<!-- -->

- use `vulns` to show information about the exploits
